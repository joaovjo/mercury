node_modules/
dist/
*.log
.DS_Store

# Generated build artifact: the base64-inlined web/dist bundle. Regenerated by
# `scripts/embed-assets.ts` on every build; a stub is created by
# `scripts/ensure-assets.ts` for source/dev runs. Committing it caused conflicts
# on parallel UI PRs (see issue #20).
src/server/assets.gen.ts
